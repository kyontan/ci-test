box: ruby:2.3.0

build:
  steps:
    - script:
        name: install nodejs / npm
        code: |
          apt-get update
          apt-get install -y nodejs
          curl -0 -L http://npmjs.org/install.sh | sudo sh
    - script:
        name: set nodejs environment
        code: |
          nodejs --version
          npm --version
          npm install -g n
          n stable
          nodejs --version
          node --version
          rm `which nodejs` && ln `which node` /usr/bin/nodejs
          nodejs --version
          node --version
    # - script:
    #     name: bower install
    #     code: |
    #       $(npm bin)/bower --allow-root install
    # - script:
    #     name: gulp build
    #     code: |
    #       $(npm bin)/gulp build
